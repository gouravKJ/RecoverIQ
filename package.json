{
  "name": "recoveriq",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/shared",
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w @recoveriq/shared && npm run build -w backend && npm run build -w frontend",
    "test": "npm run test -w backend",
    "db:up": "docker compose up -d",
    "db:migrate": "npm run db:migrate -w backend",
    "db:seed": "npm run db:seed -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
